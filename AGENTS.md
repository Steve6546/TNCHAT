# AGENTS.md — قواعد ملزمة لمن يعدّل هذا المشروع

هذا الملف مكتوب لك سواء كنت وكيل ذكاء اصطناعي أو مطوّراً بشرياً. المشروع صغير
ومتماسك، ومعظم الأخطاء التي يمكن أن تقع فيه ليست أخطاء تجميع بل **أخطاء صامتة**:
طلب يتوجّه لقناة خاطئة، تدفّق لا يُغلق، مفتاح مُخزَّن نصّاً صريحاً. القواعد
التالية تمنع تلك الأنواع تحديداً.

اقرأ هذا الملف كاملاً قبل أول تعديل. البنية والتصميم موثّقان في
**[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** — الحالة الحالية بعد 0.2.0.

---

## 1. قبل أي تعديل

```bash
node scripts/acc.mjs check
```

يجب أن يخرج بلا أخطاء. إن لم يخرج كذلك، أصلِح ذلك أولاً — لا تبنِ على أساس مكسور.

## 2. قبل أن تنهي

نفّذ `node scripts/acc.mjs check` مرة أخرى، ثم أضف اختباراً لأي سلوك غيّرته.

---

## ممنوعات مطلقة

### لا تحذف ميزة لتقليل الكود

«قلّل الكود» لا يعني «احذف وظيفة». قبل حذف أي تصدير، ابحث عن استخدامه في كل
المشروع **وفي الواجهة**. إن كان مستخدماً، اتركه. إن لم يكن، احذفه صراحةً واذكر
ذلك في رسالة التCommit.

الفحص السريع:

```bash
grep -rn "اسم_الدالة" server/src web/src
```

### لا تستبدل نظاماً قائماً بآخر

الأنظمة التالية **واحدة فقط** من كل نوع. إن وجدت طريقة ثانية لفعل نفس الشيء،
فهذا خطأ يجب إزالته — لا إضافة نظام ثانٍ:

| المسؤولية | المكان الوحيد |
| --- | --- |
| اتصال Supabase Postgres (عميل + Drizzle) | `server/src/db/index.ts` |
| تشفير/فكّ مفاتيح المزوّدين | `server/src/lib/secrets.ts` |
| إصدار/تحقّق رموز جلسة لوحة التحكم | `server/src/gateway/dashboard-auth.ts` |
| نداءات Supabase Auth (تسجيل/دخول/استرجاع) | `server/src/auth/supabase.ts` |
| تحليل JSON إلى قائمة أو خريطة نصية | `server/src/lib/json.ts` |
| اختيار القناة | `server/src/gateway/distributor.ts` |
| سلسلة إعادة توجيه النموذج | `server/src/gateway/model-mapping.ts` |
| مخطط قاعدة البيانات + الفحص | `server/src/db/migrate.ts` |
| تحميل `.env` | `server/src/config.ts` |
| تنظيف عنوان المزوّد (Base URL) | `server/src/adapters/index.ts` — `normalizeBaseUrl()` |
| ترويسات العميل المُمرَّرة للمزوّد | `server/src/gateway/upstream.ts` — `clientPassthroughHeaders()` |
| التحقق من صيغة البريد وكلمة المرور (خادم) | `server/src/lib/validate.ts` — `email()` / `password()` |
| كتالوج بطاقات النماذج | `web/src/stores/model-cards-store.ts` |
| عنوان الـ endpoint المعروض للنسخ | `web/src/lib/api.ts` — `relayEndpoint()` |
| كل أوامر التشغيل | `scripts/acc.mjs` |

### لا تُعِد إضافة نظام انتهاء للجلسة

الجلسات **بلا انتهاء** بقرار معماري: رمز موقَّع HMAC يُخزَّن في
`sessionStorage`، وينتهي بتسجيل الخروج أو بإغلاق التبويب. لا تضِف `exp`، ولا
عدّاداً تنازلياً، ولا `Countdown` مرتبطاً بالجلسات — كل نظام من هذا القبيل
حُذف عمداً (المكوّن نفسه لم يعد في المستودع).

### قاعدة البيانات Supabase فقط — وكل الاستعلامات async

- لا `better-sqlite3` ولا واجهاته المتزامنة: لا `.get()` ولا `.run()` ولا
  `.all()`. طبقة Drizzle هنا فوق `postgres-js`، فكل استعلام `await`.
- لا تُضِف محرّك قواعد بيانات ثانياً ولا طبقة «وضع محلي» — القرار: أونلاين فقط.
- الطوابع الزمنية تبقى **بالميلي ثانية** (`bigint` بوضع `mode: 'number'`
  في `schema.ts`). لا تحوّلها إلى `timestamptz`.

### لا تلمس ثوابت الخوارزمية

قيم التوجيه منقولة عن new-api ومُختبَرة ميدانياً. تغييرها يغيّر توزيع الطلبات
بطريقة يصعب ملاحظتها في الاختبارات ولا تُكتشف إلا في الإنتاج:

- `WEIGHT_OFFSET = 10` في `distributor.ts` — يمنع تجويع القنوات ذات الوزن صفر.
- ترتيب الشرائح **تنازلي**، وكل إعادة محاولة تنزل شريحة.
- `MAX_TEST_TOKENS` في `admin-channels.ts` — قيمة المجسّ الحيّ.

### لا تُبدّل نمط بناء الكود

لا تستبدل `tsc` بأداة أخرى، ولا تضيف حزمة بناء جديدة، ولا تغيّر إعدادات
`tsconfig.json` أو `vite.config.ts` إلا لسبب موثّق في رسالة الـ commit.

---

## قواعد خاصة بالأمان

### لا تسجّل ولا تُظهر أسراراً

- لا تكتب مفاتيح المزوّدين في السجلات أو رسائل الخطأ أو ردود الـ API.
- لا تُظهر محتوى `MASTER_KEY` أو `SESSION_SECRET` أبداً. نقطة `/health` تعرض
  **مصدر** كل واحد منهما (`environment` / `generated-file` / `derived`)، لا قيمته.
- مفاتيح Supabase: `SUPABASE_ANON_KEY` عام بالتصميم ويُقدَّم للمتصفح عبر
  `/api/auth/config`؛ أما `DATABASE_URL` فيحتاج كلمة مرور فلا يغادر الخادم
  أبداً. لا يُخزَّن مفتاح `service_role` في أي مكان في هذا المشروع.
- مفتاح العميل النصّي الصريح يظهر مرة واحدة فقط في استجابة الإنشاء، ولا يُعاد.

### لا تُدخل تبعية دون حاجة

كل حزمة تُضاف هي سطح هجوم وصيانة. قبل الإضافة، اسأل: هل يمكن كتابتها في أقل
من 80 سطراً؟ نظام التنبيهات (`web/src/components/ui/toast.tsx`) مكتوب يدوياً
لهذا السبب بالتحديد.

### لا تُوسّع سطح الشبكة بلا سبب

`CORS_ORIGIN` لا يقبل `*` في الإنتاج. `TRUST_PROXY` يبقى `false` إلا خلف بروكسي
موثوق. لا تُبدّل هذين الافتراضيين ليسهّلا عليك الاختبار.

---

## قواعد خاصة بالواجهة

- **لا بيانات تجريبية.** لا أرقام وهمية، ولا «قريباً». إن لم توجد بيانات،
  اعرض حالة فارغة صادقة تشرح ما سيظهر لاحقاً.
- **كل تغيير لحالة الخادم يجب أن يُخبر المستخدم.** استخدم `useToast` بنجاح أو
  فشل. النجاح الصامت لا يمكن تمييزه عن الفشل.
- **كل نص موجّه للمستخدم بالعربية، والواجهة RTL.** النصوص التقنية (أسماء
  النماذج، الروابط، الأرقام) تبقى LTR داخل عنصر `dir="ltr"`.
- **الأرقام تستخدم نظام `latn`.** دوال التنسيق في `web/src/lib/utils.ts` تثبّت
  ذلك — استخدمها ولا تكتب `Intl` يدوياً.
- **كل الطوابع الزمنية بالميلي ثانية.** مخطط قاعدة البيانات يستخدم أعمدة
  `bigint` بقيم `unixepoch * 1000`. احترم ذلك في الاتجاهين.
- **الواجهات الثقيلة تُحمَّل مؤجَّلة.** الرسم البياني في `requests-chart.tsx`
  مفصول بـ `lazy()` لأن Recharts يمثّل ~40% من حجم الحزمة. لا تستورده مباشرة.

---

## قواعد خاصة بالخادم

- **كل مسار إدارة يتطلب جلسة.** مسجل عبر `onRequest` في `app.ts`. أي مسار جديد
  تحت `/api/` مشمول تلقائياً — لا تضف استثناءً.
- **كل استجابة خطأ بصيغة واحدة**: `{ error: { message, type, code } }` عبر
  `GatewayError`. لا ترمِ `Error` عادياً من مسار.
- **`finalize()` إجباري** في نهاية أي تدفّق، داخل `finally`. بعض المحوّلات
  تُصدر حدث الإنهاء والاستخدام النهائي هناك فقط — إسقاطه يكسر العميل بصمت.
- **مسارات الـ SPA تُسجَّل أخيراً** في `app.ts`، لأنها تتضمن ملحقاً خاماً (`*`)
  سيحجب كل ما بعده.

---

## قواعد خاصة بـ Windows

المشروع يُشغَّل على Windows ويجب أن يبقى كذلك:

- **لا تمرّر مساراً عبر الصدفة.** اسم المجلد قد يحتوي `&` ومسافات، و`cmd.exe`
  يفسّر `&` كفاصل أوامر. استخدم `process.execPath` مع مصفوفة وسائط.
- **لا تستدعِ ملفات `.cmd`.** استخدم مسارات الإدخال الحقيقية (مثل
  `node_modules/typescript/bin/tsc`).
- **لا تفترض أن `kill()` يقتل الشجرة.** على Windows يقتل الابن المباشر فقط؛
  استخدم `taskkill /pid <pid> /t /f`.
- **لا تحوّل مساراً إلى `file://` بالسلسلة النصية.** استخدم `pathToFileURL`.

---

## بنية الـ commit

رسالة موجزة بصيغة الأمر، والسبب في الجسم إن كان غير بديهي:

```
fix(stream): always finalize the SSE response

Some converters emit the terminal event and final usage only from
finalize(). Skipping it made streamed requests hang on the client.
```

---

## ملخص سريع

| أريد أن… | افعل |
| --- | --- |
| أشغّل المشروع | `node scripts/acc.mjs start` |
| أتحقق من تغييري | `node scripts/acc.mjs check` |
| أعيد بناء قاعدة البيانات | `node scripts/acc.mjs reset` |
| أضيف مزوّداً علوياً | `server/src/adapters/index.ts` |
| أضيف مسار إدارة | `server/src/routes/` + التسجيل في `app.ts` |
| أضيف شاشة | `web/src/features/` + التسجيل في `app.tsx` |
| أغيّر نصاً معروضاً | الملف نفسه؛ العربية RTL إلزامية |
| أضيف حزمة | اسأل أولاً: أليست 80 سطراً تكفي؟ |
| أفهم البنية أو التصميم | `docs/ARCHITECTURE.md` |
| أطلق إصداراً | حدّث `CHANGELOG.md` و`package.json`، ثم وسم `vX.Y.Z` |
