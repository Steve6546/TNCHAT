# CHANGELOG

يتبع [Semantic Versioning](https://semver.org) ويُكتب بالعربية لأن فريق
المشروع عربي.

## [غير مُصدَر]

### أُضيف

- **عدّاد تنازلي حقيقي للجلسة.** كان نص «12 ساعة» ثابتاً ومضلّلاً: لا يعكس عمر
  الجلسة الفعلي، ولا يتغيّر، ولا ينبّه عند انتهائها. صار عدّاداً حيّاً
  بهيئة `11:59:59` ينقص كل ثانية، في ثلاثة مواضع: شاشة الدخول، وتذييل الشريط
  الجانبي، وصفحة الإعدادات عند تغيير كلمة المرور.
  - القراءة من **المصدر الحقيقي**: مكوّن `SessionRemaining` يفكّ ترميز حمولة
    الرمز على العميل ويقرأ `exp`، بدل افتراض مدة ثابتة. إذا تغيّرت مدة الجلسة
    في الخادم، يتبعها العدّاد تلقائياً.
  - **لا انحراف زمني**: العدّاد يعيد قراءة `Date.now()` في كل نبضة بدل إنقاص
    عدّاد محفوظ، فلا يتراكم التأخير عندما تنام علامة التبويب أو يضغط النظام
    المؤقتات.
  - **يعكس حالة الخادم**: عند بلوغ الصفر في الشريط الجانبي يُستدعى تسجيل
    الخروج، فلا تبقى شاشة تعرض بيانات مرفوضة. على شاشة الدخول يُعاد ضبط
    العدّاد، لأن الجلسة لم تبدأ بعد.
  - يتحوّل إلى البرتقالي في آخر 5 دقائق وإلى الأحمر عند الانتهاء.
- **إظهار/إخفاء كلمة المرور.** حقل `PasswordField` جديد بزرّ عين في كل حقول
  كلمة المرور (الدخول، الإعداد الأولى، تغيير كلمة المرور — Current/New/Confirm).
  الزرّ من نوع `button` فلا يُرسل النموذج بالخطأ، وموضعه على الحافة الخلفية في
  RTL و LTR معاً.
- **انتهاء الجلسة من الخادم، لا من المتصفح.** كان العدّاد يحسب من ساعة الجهاز،
  فأي فرق بينها وبين ساعة الخادم يظهر خطأً في الوقت المتبقي. صار الخادم يردّ
  `expiresAt` بصيغة ISO مع `serverTime` عند الدخول والإعداد الأولى، وتقيس
  الواجهة الفرق بينهما مرة واحدة ثم تحسب المتبقي من ساعة الخادم. عند بلوغ
  `00:00:00` يُحذف الرمز ويُوجَّه المستخدم إلى شاشة الدخول مع تنبيه.
- **إدارة السجلات.** زر «مسح السجلات» في بطاقة أحدث الطلبات بخيارين: حذف
  الأخطاء فقط (502/503 وأمثالها) أو حذف كل السجلات. وفلتر سريع «الناجحة فقط»
  يعرض صفوف 200 OK وحدها، مع حالة فارغة صادقة إن كانت كل الطلبات الأخيرة فاشلة.
- **نتيجة الفحص داخل الصف.** زر الفحص بجانب القناة لم يكتفِ بالتنبيه: صار
  يعرض تحت الصف نفسه حالة الاستجابة ورمزها وزمنها واسم النموذج الذي جُرّب.
- **نسخ المفتاح مع الـ Endpoint بنقرة واحدة.** في نافذة ظهور المفتاح، زرّ ينسخ
  المفتاح ورابط `…/v1` معاً — وهما ما يُلصق معاً دائماً — ويُظهر تنبيه نجاح.
  الرابط مشتق من أصل الصفحة الجاري، فيصحّ على أي منفذ يعمل عليه الخادم.
- **بطاقات النماذج بدل كتابة التحويل.** قسم «ربط النماذج بالبطاقات» في نموذج
  القناة: بطاقة لكل اسم يطلبه العملاء عادةً، بتفعيل بضغطة زر. تفعيل البطاقة
  يُعلن الاسم على القناة، وإن اختلف اسمه عند المزوّد يُكتب سطر التحويل
  `من -> إلى` في الخلفية — دون كتابة الصيغة يدوياً.
- **بطاقات قابلة للتخصيص بالكامل.** زر «إضافة بطاقة جديدة» لأي نموذج مستقبلي،
  وتعديل بالضغط على أي بطاقة (الاسم والوصف)، وحذف بتأكيد داخل البطاقة نفسها.
  البطاقات محفوظة في متصفحك، وعند تحديث القائمة الافتراضية تُضاف النماذج
  الجديدة دون أن تُمحى تعديلاتك. القائمة الافتراضية الآن تضم `claude-opus-5` و
  `claude-3-7-sonnet` و `claude-3-5-sonnet` و `claude-3-5-haiku`.
- **دليل التشغيل داخل الإعدادات.** تبويب «دليل التشغيل» يشرح ربط أي تطبيق
  بالبوابة بخطوات ثلاث، ويعرض العنوان والمسارات المتاحة مشتقّة من الخادم الجاري،
  مع أمثلة جاهزة للنسخ (cURL و Python و Node وواجهة Claude) وجدول لرموز
  الأخطاء 401/403/502/503/429 ومعناها وكيف تصلحها.

### تغيير

- **لون التفعيل صار أخضر.** كان الزرّ عند التشغيل يتحوّل إلى الأبيض لأن
  `--primary` في الوضع الداكن هو لون النصّ نفسه (`#fafafa`)، فيصبح الزرّ
  والمؤشّر كتلة بيضاء واحدة. أُضيف رمز لون `--success` (أخضر هادئ) وصار
  التفعيل يستخدمه في الوضعين.
- **حقل «النماذج المسموحة» حُذف من نافذة إنشاء المفتاح.** النماذج تُحدَّد من
  القناة، فوجود الحقل هنا كان تكراراً بلا أثر. النافذة الآن: اسم، وتاريخ
  انتهاء بأزرار سريعة (30/90 يوماً أو سنة أو بدون انتهاء)، وزر إنشاء. المفاتيح
  القديمة ذات القيود تبقى محكومة بها.
- **حقل «النماذج» النصّي حُذف من نموذج القناة.** كان يعبّر عن نفس بيانات
  البطاقات بطريقة ثانية، فصار مصدراً للتعارض. البطاقات الآن هي المدخل الوحيد،
  وأي نموذج محفوظ على القناة وليس له بطاقة يظهر كبطاقة «مُضاف على هذه القناة»
  قابلة للتعديل والحذف.

### تغييرات داخلية

- `web/src/lib/session.ts`: فكّ ترميز `base64url` لحمولة الرمز واستخراج `exp`،
  مع قيمة احتياطية عند غياب الرمز أو فساد الحمولة. أُضيفت `clockSkew` و
  `sessionDeadline` لحساب المتبقي على ساعة الخادم.
- `web/src/components/shared/countdown.tsx`: `useCountdown` و `formatCountdown`
  ومكوّن `Countdown` — بلا تبعيات خارجية.
- `web/src/components/ui/password-field.tsx`: حقل كلمة مرور بإظهار/إخفاء.
- `server/src/gateway/dashboard-auth.ts`: `login()` يردّ `{ token, expiresAt }`
  بدل سلسلة نصية، و`TOKEN_TTL_HOURS` مُصدَّر مع `TOKEN_TTL_MS`.
- `server/src/routes/admin-auth.ts`: `sessionPayload()` يضمّ `expiresAt` و
  `serverTime`؛ و`/api/auth/status` يردّ `serverTime` أيضاً.
- `server/src/routes/admin-stats.ts`: `DELETE /api/stats/logs?scope=errors|all`
  و`?ok=1|0` على `/api/stats/overview` (يصفّي جدول أحدث الطلبات فقط).
- `web/src/stores/auth-store.ts`: الجلسة تحفظ `expiresAt` و`skewMs` مع الرمز.
- `web/src/features/channels/model-cards.tsx`: كتالوج البطاقات ومكوّنها.
- `web/src/features/channels/channels-page.tsx`: تحقّق من صيغة `->` لكل سطر
  تحويل (يمنع المسافة العادية)، وقائمة الأنواع مقصورة على المدعوم، ونتيجة
  الفحص محفوظة لكل قناة.
- `web/src/lib/api.ts`: `relayEndpoint()` و`clearLogs()` وفلتر `statsOverview`.
- `web/src/index.css`: رمز `--success` (فاتح `#059669` / داكن `#10b981`) مضاف
  إلى `:root` و`.dark` ومُسجَّل في `@theme inline` كـ `--color-success`.
- `web/src/components/ui/switch.tsx`: حالة التفعيل تستخدم `bg-success`،
  والمؤشّر أبيض ثابت، مع حلقة تركيز للوحة المفاتيح.
- `web/src/stores/model-cards-store.ts`: كتالوج البطاقات محفوظ في
  `localStorage` مع `version` و`migrateModelCards()` التي تُلحق النماذج
  الافتراضية الجديدة بدل استبدال القائمة.
- `web/src/features/channels/model-cards.tsx`: مكوّن البطاقات صار ديناميكياً
  (إضافة/تعديل/حذف) ويشتق بطاقات إضافية من حقل `models` حتى لا يخفي ما تخدمه
  القناة فعلاً.
- `web/src/features/settings/setup-guide.tsx`: دليل التشغيل، بعناوين وأمثلة
  مشتقّة من `relayEndpoint()` بدل ثوابت مكتوبة.

## [0.1.0] — 2026-08-30

أول إصدار منظّم. البوابة تعمل، لوحة التحكم كاملة، والمشروع يُشغَّل بأمر واحد.

### أُضيف

- **مشغّل موحّد** (`scripts/acc.mjs`): أمر واحد `node scripts/acc.mjs start`
  يثبّت الاعتمادات وينسخ `.env` ويهيّئ قاعدة البيانات ويبني ويشغّل كل شيء على
  منفذ واحد. يعمل على Windows رغم وجود `&` ومسافة في اسم المجلد، لأنه لا يمرّر
  أي مسار عبر الصدفة.
- **خدمة الواجهة من البوابة نفسها** عبر `@fastify/static`، مع إرجاع
  `index.html` للروابط العميقة، و`404` بصيغة JSON لمسارات `/api/` و `/v1/`.
- **لوحة تحكم عربية RTL**: نظرة عامة، إدارة النماذج، مفاتيح API، الإعدادات.
  كل القيم من بيانات حقيقية — لا أرقام تجريبية في أي مكان.
- **نظام تنبيهات** مكتوب يدوياً (~70 سطراً) بدل إضافة تبعية.
- **مفتاح الجلسة مشتق عبر HKDF** من `MASTER_KEY` بهدف منفصل، فلا يمكن تحويل
  جلسة مسرّبة إلى قدرة على فكّ تشفير مفاتيح المزوّدين.
- **تحديد معدّل** على تسجيل الدخول ونقطة الإعداد الأولى، مع احترام
  `X-Forwarded-For` فقط عند تفعيل `TRUST_PROXY`.
- **ترويسات أمان** على كل الردود، ورفض `*` في CORS أثناء الإنتاج.
- **`configSummary()`** في `/health`: ملخّص آمن للإعداد الجاري، يعرض مصدر كل
  سرّ دون قيمته.
- **مستندات**: `README.md`، `AGENTS.md` (قواعد ملزمة للوكلاء والمطوّرين)،
  `CONTRIBUTING.md`.
- **اختبارات**: 9 اختبارات تغطّي تحويل الطلبات والردود وأحداث التدفّق بين
  صيغتي OpenAI و Claude.

### تحسين

- **فهرس التوجيه يُبنى مسبقاً**: شرائح الأولوية وخرائط المجموعة تُحسب مرة واحدة
  في `rebuild()`، فيصبح اختيار القناة بحثاً في خريطة بدل فرز وتصفية لكل طلب.
- **حجم حزمة الواجهة انخفض من 762 KB إلى 400 KB** (gzip من 234 KB إلى
  126 KB) بفصل الرسم البياني في وحدة مُحمَّلة مؤجَّلاً — Recharts كان يمثّل
  ~40% من الحجم ولا فائدة منه قبل وجود بيانات.
- **مصدر واحد للإعداد**: `.env` يُقرأ من جذر المشروع فقط. كان هناك ملف
  `server/.env` ثانٍ يخلق لبساً حول أي قيمة تسود.
- **ثلاثة محوّلات متطابقة** في `adapters/index.ts` دُمجت في دالة
  `openaiCompatible(kind, label)`.

### إصلاح

- **البناء يفشل على نسخة نظيفة.** كان `.npmrc` يضبط `hoist=false` لتسريع
  التثبيت على Windows. هذا يمنع pnpm من إنشاء مزرعة الروابط المرفوعة
  (`node_modules/.pnpm/node_modules`)، وهي المكان الوحيد الذي تتعرّض منه
  الحزم التابعة لـ `fastify` كـ peer dependency. بدونه لا تستطيع تعريفات
  `@fastify/static` إيجاد `fastify`، فلا يُطبَّق توسيع الوحدة الذي يضيف
  `reply.sendFile`، ويفشل `tsc`. الخطأ كان غير مرئي على الأجهزة التي تحوي
  `node_modules` من تثبيت سابق — وهو سبب وصوله إلى المستودع. **يُتحقق الآن
  بنسخة نظيفة، لا بتثبيت دافئ.**
- **كلمة مرور افتراضية منشورة.** كان `.env.example` يحمل قيمة
  `ADMIN_PASSWORD` عاملة، والمشغّل ينسخ هذا الملف إلى `.env` في أول تشغيل —
  أي أن كل نسخة جديدة من المستودع العام تحصل على كلمة مرور لوحة التحكم نفسها،
  المنشورة في المستودع. القيمة فارغة الآن، والتثبيت النظيف يمرّ فعلياً بشاشة
  الإعداد الأولى.
- **تناقض بين `/api/auth/status` ونقطة الإعداد.** كانت نقطة الإعداد تتحقق من
  وجود هاش مخزّن فقط، بينما `/api/auth/status` يعتبر `ADMIN_PASSWORD` من
  البيئة كافياً. النتيجة: الواجهة تعرض نموذج دخول بينما الإعداد لا يزال
  مفتوحاً، ويبقى النظام بكلمة مرور لم يخترها أحد. النقطتان تستخدمان الآن
  المعيار نفسه.
- **مسار `bootstrap.ts` لم يكن يُستدعى أبداً.** كان المشغّل ينفّذ
  `migrate.ts` فقط، فلا تُرقّى كلمة المرور من البيئة ولا يُبنى فهرس التوجيه
  مسبقاً. صار `bootstrap.ts` هو نقطة الإعداد لكل ما يُشغَّل، و`migrate.ts`
  يبقى المخطط فقط (وما يشغّله `acc.mjs db`).
- **`distributor.ts` كان يحتوي بايتات `NUL` حرفية** (`0x00`) مستخدمة كفواصل في
  مفاتيح الارتباط، ما جعل الملف غير قابل للقراءة كنص. استُبدلت بفاصل الوحدة
  `\u001f`.
- **مسار جذر المشروع كان يُحسب بعدد ثابت من `..`**، فيختلف بين التطوير
  (`server/src`) والإنتاج (`server/dist`) ويضع قاعدة البيانات في مكانين
  مختلفين. صار يُكتشف بالبحث عن `pnpm-workspace.yaml`.
- **`reasonToOpenAI` / `reasonToClaude` كانا يمرّران أسباباً غير معروفة** بدل
  قيمة الإنهاء الطبيعية لكل بروتوكول.
- **استُبدل 5 محلّلات JSON مكرّرة** بـ `lib/json.ts`، و**منطق فكّ تشفير
  مكرّر** بـ `lib/secrets.ts`، و**حمولة مجسّ مكرّرة** في `admin-channels.ts`.

### تغييرات داخلية

- `.gitignore` يستثني `.env` وقاعدة البيانات والمفتاح الرئيسي ومجلدات العمل
  المؤقتة، فلا يدخل أي سرّ المستودع.
- `pnpm-workspace.yaml` يستخدم catalogs لتوحيد إصدار TypeScript بين الحزم،
  و`onlyBuiltDependencies` لحصر البناء الأصلي على `better-sqlite3`.

---

### ملاحظة على التغطية

التحويل بين **Claude ⇄ OpenAI** مطبّق في الاتجاهين. التحويل عبر **Gemini**
غير مطبّق عن قصد: المصفوفة الرسمية في `relaykit` تصنّفه `Discouraged`، وتوثيق
الحدود أصحّ من ادّعاء الاكتمال.
