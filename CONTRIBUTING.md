# CONTRIBUTING

شكراً لمساهمتك. هذا المشروع صغير ومتماسك عن قصد، وأهم ما يحميه هو القيود
المذكورة هنا.

## قبل أن تبدأ

1. اقرأ **[AGENTS.md](AGENTS.md)** — يحتوي القواعد الملزمة، بما فيها الممنوعات
   المتعلقة بالخوارزميات والأمان وWindows. ينطبق على المطوّرين البشريين
   وعلى وكلاء الذكاء الاصطناعي سواءً.
2. شغّل المشروع: `node scripts/acc.mjs start`.
3. تأكد أن الأساس سليم: `node scripts/acc.mjs check`.

## دورة التطوير

```bash
node scripts/acc.mjs dev     # خادم + واجهة مع إعادة تحميل تلقائي
node scripts/acc.mjs test    # أثناء العمل
node scripts/acc.mjs check   # قبل الـ commit — فحص الأنواع + الاختبارات
```

`check` هو ما تشغّله CI. إن لم ينجح محلياً فلن ينجح هناك.

## ما نتوقعه من مساهمة

- **لا تحذف ميزة لتقليل الكود.** هذا أكثر خطأ يقع فيه من يُطلب منه «تبسيط»
  المشروع. إن أردت إزالة تصدير، ابحث عن استخدامه أولاً.
- **اختبر ما غيّرته.** الاختبارات الحالية (42) تغطّي التحويل بين صيغتي OpenAI
  و Claude بما فيها التدفّق، ومرور طلبات Claude Code دون تعديل، ونوع القناة
  المخصّص. أي تعديل على `server/src/convert/` أو `server/src/gateway/` يحتاج
  اختباراً.
- **لا بيانات تجريبية في الواجهة.** حالة فارغة صادقة أفضل من رقم مزيّف.
- **النصوص المعروضة بالعربية**، والواجهة RTL. النصوص التقنية تبقى `dir="ltr"`.
- **لا تسرّب أسراراً.** لا ترفع `.env`، ولا تكتب مفتاحاً في سجل أو رسالة خطأ،
  ولا تعرض محتوى `MASTER_KEY`.

## نمط الكود

- TypeScript صارم في كل مكان.
- لا توجد أداة تنسيق مُهيّأة — طابق النمط الموجود في الملف الذي تعدّله.
- مسافة بادئة: مسافتان. فواصل زائدة في نهاية القوائم المتعددة الأسطر.
- التعليقات تشرح **لماذا**، لا **ماذا**. الكود يوضّح ماذا يفعل.

## إضافة مزوّد علوي

معظم المزوّدين يقدّمون نقطة نهاية متوافقة مع OpenAI. إن كان مزوّدك كذلك، أضفه
عبر `openaiCompatible(kind, label)` في `server/src/adapters/index.ts`، وأضف
النوع إلى `ChannelType`.

**لا تُضِف تحويلات تخمينية بناءً على التوثيق وحده.** تعليق في ذلك الملف يوثّق
حالة سابقة: إزالة بادئة `MiniMaxAI/` كانت موصّى بها في التوثيق الرسمي، لكن
النقطة الحقيقية ترد 404. القاعدة: السلوك الخاص بالمزوّد يُضاف بعد ملاحظته على
طلب حقيقي، لا قبله.

## رسائل الـ Commit

صيغة الأمر، مع نوع بين قوسين، والسبب في الجسم عند الحاجة:

```
feat(channels): add per-channel weight in the advanced panel
fix(stream): always finalize the SSE response
docs(readme): document the single start command
```

## الإبلاغ عن مشكلة

اذكر: نظام التشغيل، إصدار Node، الخطوات لإعادة إنتاج المشكلة، وما توقّعت أن
يحدث مقابل ما حدث فعلاً. السجلات تساعد — احذف منها أي مفاتيح قبل لصقها.
